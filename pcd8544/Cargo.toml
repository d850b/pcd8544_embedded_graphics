[package]
name = "pcd8544"
version = "0.1.0"
edition = "2024"

[features]
defmt  = ["dep:defmt"]
#log = ["dep:log"]


[dependencies]

defmt = { workspace = true, optional = true }
#log = { workspace = true, optional = true }
